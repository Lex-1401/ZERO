#!/usr/bin/expect -f
set timeout 30
set password "bMjVTAVByf"
set host "74.1.20.119"

spawn ssh root@$host "cat /proc/46969/environ | tr '\\0' '\\n' | grep ZERO_GATEWAY"
expect "password:" { send "$password\r" }
expect {
    timeout { exit 0 }
    eof { exit 0 }
}
