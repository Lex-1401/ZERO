/* tslint:disable */
/* eslint-disable */

/* auto-generated by NAPI-RS */

export class SecurityEngine {
    constructor()
    encrypt(plaintext: string): string
    decrypt(ciphertext: string): string
    redact(text: string, context?: string): string
    redactPii(text: string): string
    validate(text: string): boolean
    verifyIntegrity(data: Buffer): boolean
    calculateEntropy(text: string): number
    calculateDensity(text: string): number
    detectInjection(text: string): InjectionResult
}

export interface InjectionResult {
    detected: boolean
    confidence: number
    reason?: string
}

export class VadEngine {
    constructor(threshold: number, sensitivity: number)
    processChunk(buffer: Buffer): "speech" | "silence" | "unknown"
}

export class RatchetDedupe {
    constructor(ttlMs: number, maxSize: number)
    check(key: string, now?: number): boolean
    clear(): void
    size(): number
}

// Panic Functionality
export function triggerPanic(reason: string): void
export function resetPanic(): void
export function isPanicMode(): boolean

// Metrics
export class MetricsEngine {
    constructor()
    recordTokens(model: string, count: number): void
    recordLatency(ms: number): void
    summarize(): MetricsSummary
}

export interface MetricsSummary {
    tokens: Record<string, number>
    p95Latency: number
    p99Latency: number
}

export function getSystemMetrics(): SystemMetrics

export interface SystemMetrics {
    cpuUsage: number
    memoryUsage: number
    uptime: number
}
