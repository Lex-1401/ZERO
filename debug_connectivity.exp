#!/usr/bin/expect -f
set timeout 30
spawn ssh -o StrictHostKeyChecking=no root@74.1.20.119
expect "password:"
send "7SIiZRHvPD\r"
expect "#"
# Check logs specifically for WebSocket connections or errors
send "grep -i 'websocket' /var/log/zero.log | tail -n 20\r"
expect "#"
# Check if PM2 logs have different info
send "pm2 logs zero-gateway --lines 50 --nostream\r"
expect "#"
# Check network connections on port 3000
send "netstat -tnap | grep :3000\r"
expect "#"
send "exit\r"
