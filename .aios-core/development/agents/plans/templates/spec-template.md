# 📐 SPEC: [NOME_DA_FUNCIONALIDADE]

## 🏗️ 1. ARCHITECTURAL PLAN

- **Strategic Approach:** Como vamos acoplar essa nova lógica sem ferir o `rust-core`.
- **Data Flow:** Mapeamento de como os dados entram e saem desta funcionalidade.

## 📂 2. FILE MODIFICATIONS (TACTICAL LIST)

| Arquivo | Ação | Descrição da Modificação |
| :--- | :--- | :--- |
| `path/to/file` | Edit | Descrição exata do que será alterado. |
| `path/to/new` | Create | Estrutura básica do novo arquivo. |

## 🧪 3. TEST PLAN

- **Unit Tests:** Casos de borda (edge cases) a serem cobertos.
- **Integration:** Como garantir que o `realtime-agent` ou o `aios-core` não parem de responder.

## 🏁 4. IMPLEMENTATION CHECKLIST (STEP-BY-STEP)

1. [ ] Setup de variáveis de ambiente/infra.
2. [ ] Modificação de schemas/tipagem.
3. [ ] Implementação da lógica principal.
4. [ ] Validação visual (8pt grid check).
5. [ ] Gate de Qualidade Master (`pnpm validate`).

---
> **Ação:** Spec criada. Favor limpar janela de contexto (clear context) para iniciar o Coding.
