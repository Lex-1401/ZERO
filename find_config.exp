#!/usr/bin/expect -f
set timeout 30
set password "7SIiZRHvPD"
set host "74.1.20.119"

spawn ssh root@$host "find / -name zero.json 2>/dev/null"
expect "password:" { send "$password\r" }
expect {
    timeout { exit 0 }
    eof { exit 0 }
}
