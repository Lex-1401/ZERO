
export async function applyOpenAiConfig(_params: any): Promise<any> {
    // Logic to prompt for OpenAI API key and validate it.
    return { ok: true };
}

export async function applyAnthropicConfig(_params: any): Promise<any> {
    // Logic to prompt for Anthropic API key and validate it.
    return { ok: true };
}
