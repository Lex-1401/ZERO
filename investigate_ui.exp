#!/usr/bin/expect -f
set timeout 30
spawn ssh -o StrictHostKeyChecking=no root@74.1.20.119
expect "password:"
send "7SIiZRHvPD\r"
expect "#"
# Check if nginx is running
send "ps aux | grep nginx\r"
expect "#"
# Check the timestamp of the UI index.html to see if it was updated
send "ls -l /opt/zero/dist/control-ui/index.html\r"
expect "#"
# List the assets directory to see if new chunks are there
send "ls -lt /opt/zero/dist/control-ui/assets | head -n 10\r"
expect "#"
send "exit\r"
