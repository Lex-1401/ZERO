---
summary: "Referência CLI para `zero reset` (resetar estado/config local)"
read_when:
  - Você quer limpar o estado local mantendo a CLI instalada
  - Você quer um dry-run do que seria removido
---

# `zero reset`

Resete config/estado local (mantém a CLI instalada).

```bash
zero reset
zero reset --dry-run
zero reset --scope config+creds+sessions --yes --non-interactive
```
