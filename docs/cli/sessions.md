---
summary: "Referência CLI para `zero sessions` (listar sessões armazenadas + uso)"
read_when:
  - Você quer listar sessões armazenadas e ver atividade recente
---

# `zero sessions`

Liste sessões de conversa armazenadas.

```bash
zero sessions
zero sessions --active 120
zero sessions --json
```
