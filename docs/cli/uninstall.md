---
summary: "Referência CLI para `zero uninstall` (remover serviço gateway + dados locais)"
read_when:
  - Você quer remover o serviço gateway e/ou estado local
  - Você quer um dry-run primeiro
---

# `zero uninstall`

Desinstale o serviço gateway + dados locais (CLI permanece).

```bash
zero uninstall
zero uninstall --all --yes
zero uninstall --dry-run
```
