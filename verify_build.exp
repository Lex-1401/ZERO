#!/usr/bin/expect -f
set timeout 30
spawn ssh -o StrictHostKeyChecking=no root@74.1.20.119
expect "password:"
send "bMjVTAVByf\r"
expect "#"
# Search for the v2 key in the built assets
send "grep -r 'zero.control.settings.v2' /opt/zero/dist/control-ui/assets/\r"
expect "#"
# Check the timestamp of the assets
send "ls -lat /opt/zero/dist/control-ui/assets/ | head -n 5\r"
expect "#"
send "exit\r"
